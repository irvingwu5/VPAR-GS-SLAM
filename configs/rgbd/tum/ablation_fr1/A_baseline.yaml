# Ablation A: Baseline - VO disabled, original 100 tracking iterations
inherit_from: "configs/rgbd/tum/fr1_desk.yaml"

# Enable Surface-aware Depth supervision
surface_depth:
  use_surface_depth: false
  depth_type: "median"
  dist_type: "original"

opt_params:
  lambda_normal: 0.0

VOPrior:
  enabled: False

Training:
  tracking_itr_num: 100
